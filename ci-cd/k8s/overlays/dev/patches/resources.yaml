# Dev resource limits - lower resources for cost savings
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: travel-web
spec:
  template:
    spec:
      containers:
        - name: travel-web
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
