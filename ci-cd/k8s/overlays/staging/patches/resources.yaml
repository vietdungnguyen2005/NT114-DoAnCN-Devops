# Staging resource limits - moderate resources
apiVersion: apps/v1
kind: Deployment
metadata:
  name: travel-web
spec:
  template:
    spec:
      containers:
        - name: travel-web
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
