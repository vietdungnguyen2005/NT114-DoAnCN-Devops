# Production resource limits - higher resources for performance
apiVersion: apps/v1
kind: Deployment
metadata:
  name: travel-web
spec:
  template:
    spec:
      containers:
        - name: travel-web
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
